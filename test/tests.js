const test = QUnit.test;

QUnit.testStart(() => {
    sessionStorage.clear();
});

test('test', (assert) => {
    // arrange
    

    // act
    

    // assert
    assert.deepEqual(result, expected);
});